export type BtnPropsType = {
    title: string;
    bgColor: string;
    textColor: string;
    onClickHandle?: () => void;
};

export type DropdownpPropsType = {
    title: string;
    options?: string[];
};
